/**
 * Launch market: Abu Dhabi, UAE.
 * Used ONLY as a fallback map center when no GPS position and no user-picked
 * location are available. Never overrides a real user location.
 */
export const ABU_DHABI = { lat: 24.4539, lng: 54.3773 } as const;
export const ABU_DHABI_LABEL = "Abu Dhabi, UAE";
